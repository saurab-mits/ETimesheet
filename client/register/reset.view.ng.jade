md-content(layout='row')
      span(flex='')
      md-whiteframe(layout='column', flex='50')
        div(layout='row', layout-align='space-around center')
        .md-warn(ng-show='rpc.error')
          small {{ rpc.error }}
        form(layout='column')
          md-input-container
            md-icon(md-svg-icon='content:ic_mail_24px')
            input(type='text', ng-model='email', placeholder='email', aria-label='email',value="{{user[0].emails.address}}")
          md-input-container
            md-icon(md-svg-icon='content:ic_mail_24px')
            input(ng-model='newPlaintextPassword', placeholder='New Password' required='',aria-label='password',type='password')
          md-button.md-raised.md-primary(ng-click='reset()', aria-label='login')
            | Reset password
          md-button.md-raised.md-primary(ng-click='cancel()', aria-label='login')
            | cancel
      span(flex='')
        
